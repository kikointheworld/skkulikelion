function add(num1, num2) {
  return num1 + num2;
}

const name = "김선재";
console.log(`hello ${name} !`);

// 객체 실습

const sample = {
  "key with space": true,
};

const likelion = {
  name: "멋쟁이",
  age: 12,
};

console.log(likelion["name"]);

const numArray = [1, 2, 3, 4, 5];

// 배열에 값 넣기? 배열.push(값)
//배열의 크기?  배열.length
